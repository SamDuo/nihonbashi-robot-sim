# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Notebooks
.ipynb_checkpoints/
*.ipynb_meta

# Secrets / env
.env
.env.local
*.pem
*.key
secrets.json

# Simulation outputs (large; reference in docs instead)
sim/runs/*/raw/
sim/runs/*/*.csv.zip
sim/runs/*/trace_*.xml
*.log

# CAD / 3D / large binaries
*.f3d
*.f3z
*.3dm
*.blend1
*.fbx
*.usd
*.usda
*.usdc
*.usdz

# GIS large files (use OneDrive/ArcGIS Online for sharing instead)
*.gdb/
*.shp
*.shx
*.dbf
*.sbn
*.sbx
*.tif
*.tiff
*.las
*.laz
data/heat_risk/raw/
data/pedestrian/raw/

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*~

# Build / dist
dist/
build/
*.egg

# Outputs that should be regenerated, not committed
outputs/figures/auto/
outputs/reports/auto/
# Stage One testbed generated artifacts (regenerated by scripts/run_testbed.py)
outputs/geo/
outputs/timeseries/
outputs/networks/
# WebGL twin data (regenerated by scripts/export_twin_frames.py)
outputs/twin/
# Raw Overpass response cache (8 MB; reprocess with build_city_geometry.py --offline)
data/network/raw_overpass_cache.json
outputs/reports/provenance.jsonl
outputs/reports/cooling_gap_residuals.csv
outputs/reports/testbed_comparison.md
# Local cache (OSM tiles, Mesa, etc.)
cache/
# TerriaJS catalog is regenerated by scripts/start_terriajs_demo.py; not committed
outputs/terriajs/
# Cesium diagnostic page (kept local only)
outputs/cesium_test.html

# --- PLATEAU 3D city source data (too big for GitHub; keep durable copies elsewhere) ---
# 2.4GB raw library + the extracted per-tile sample. NEVER pushable (GitHub 100MB limit).
data/modelLib.zip
data/*.zip
/model/
# Node deps (puppeteer/gltf-transform, hundreds of MB)
node_modules/
# The optimized demo city mesh (~18MB, under the limit) IS committed via `git add -f`
# even though outputs/twin/ is ignored above — so the twin works on a fresh clone.

# --- Energy twin: heavy/redundant raw uploads (processed outputs ARE committed) ---
data/energy/*.zip
data/energy/Nihonbashi_District/
data/energy/pings_in_area_6677.geojson
*.pptx
