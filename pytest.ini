[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast pure-function tests, no I/O
    integration: hits filesystem, runs Mesa, slower
addopts = -ra --strict-markers
